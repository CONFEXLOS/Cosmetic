/**
 * Configuration publique de BeautyLink Africa.
 * Ce fichier ne contient aucun secret. Pour une connexion Supabase locale,
 * copiez config.example.js vers config.local.js et chargez-le après config.js.
 */
window.BEAUTYLINK_CONFIG = Object.freeze({
  mode: 'demo',
  supabaseUrl: '',
  supabaseAnonKey: '',
  country: 'SN',
  currency: 'XOF',
  locale: 'fr-SN',
});
